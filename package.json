{
  "name": "pragathi-wellness-centre",
  "version": "2.0.0",
  "private": true,
  "description": "MERN website for Pragathi's Wellness Centre.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "postinstall": "npm run build",
    "start": "npm run start --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mongodb": "^7.2.0"
  }
}
